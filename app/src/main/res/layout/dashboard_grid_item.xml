<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@drawable/dashboard_grid_item_bg"
    android:paddingLeft="@dimen/dashboard_grid_item_paddingLeft"
    android:paddingTop="@dimen/dashboard_grid_item_paddingTop"
    android:paddingRight="@dimen/dashboard_grid_item_paddingRight"
    android:paddingBottom="@dimen/dashboard_grid_item_paddingBottom">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/icon_imageview"
            style="@style/dashboard_item_icon"
            android:layout_gravity="center" />

        <TextView
            android:id="@+id/label_textview"
            style="@style/dashboard_item_label"
            android:textSize="@dimen/dashboard_item_label_textSize" />

        <!-- classname (invisible view) -->
        <TextView
            android:id="@+id/classname_textview"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="gone" />
    </LinearLayout>
</FrameLayout>